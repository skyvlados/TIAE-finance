# frozen_string_literal: true

wb = xlsx_package.workbook
wb.add_worksheet(name: 'Operations') do |sheet|
  sheet.add_row %w[ID Category Amount Currency Date]
  @operations.each do |operation|
    sheet.add_row [operation.id, operation.category.name, operation.amount, operation.currency, operation.date]
  end
end
